/*
func sum(arg1 = 10, arg2 = 10)
{
    if ( arg1 > 250 )
    {
        return arg1 * arg1;
    }

    return arg1 + arg2 + arg1;
}
*/

//int bir = 10;

int bir, iki, uc = 5;

int bir = 1, uc = 3, bes = 5;

int number = 25;

int num2 = number;

bool nbr = bir == iki;

return 15;

bool degisken = (
    1 == 2 || 2 == 1 || true == false
);

func sum(int arg1, int arg2) => int
{
    if ( arg1 > arg2 )
    {
        return 0;
    }

    return arg1 + arg2;
}

int first = sum();
int second = sum(100, 100);
int third = sum(250, 250);
int fourth = sum(500, 500);

//arr array = ['elemanlar', first, second, third, fourth, sum(1, 2, 3, 4, 5)];